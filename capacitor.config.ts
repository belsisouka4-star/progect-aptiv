import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.company.piecesmanager',
  appName: 'Pieces Manager',
  webDir: 'build'
};

export default config;
